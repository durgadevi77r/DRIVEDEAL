// Frontend API Configuration
// Replace with your actual production backend URL after deploying the backend
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000';

export default API_BASE_URL;
